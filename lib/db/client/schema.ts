import fullSchema from "../schema";

export const schema = {
  users: fullSchema.users,
  groups: fullSchema.groups,
  userGroups: fullSchema.userGroups,
  expenses: fullSchema.expenses,
  expenseShares: fullSchema.expenseShares,
};

export default schema;